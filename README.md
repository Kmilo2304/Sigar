# SIGAR

Project structure:

- `frontend/` React + TypeScript portal UI
- `backend/` FastAPI + SQLAlchemy API and database modules
- Railway PostgreSQL configured via `backend/.env` using private domain
